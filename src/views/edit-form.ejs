<!DOCTYPE html>
<html lang="pt-br">
    <%- include('partials/head.ejs') %>
    <body>
        <div class="container">

            <div class="title-box">
            <h1>Projeto: Lista de contatos</h1>
            <p class="lead">Projeto de uma lista de contatos implementado com Node.js<br> 
                O objetivo é estruturar uma aplicação minimalista de lista de contatos no padrão MVC.</p>
            <h2>Página de cadastro</h2>
            </div>
            <a type="button" class="btn btn-secondary"  href="/">Página inicial</a>
            <br>
            <br>

            <% if (typeof message !== 'undefined') { %>
              <div class="alert alert-success" role="alert">
                <%= message %> 
              </div>
            <% } %>

            <form action="/form/update/<%=contact.id%>" method="post">
                <div class="form-row">
                  <div class="form-group col-md-6">
                    <label>Nome</label>
                    <input type="text" class="form-control" name="name" value="  <%= contact.name %>">
                  </div>
                  <div class="form-group col-md-6">
                    <label>Empresa</label>
                    <input type="text" class="form-control" name="company" value="<%= contact.company %>">
                  </div>
                  <div class="form-group col-md-6">
                    <label>Telefone</label>
                    <input type="phone" class="form-control" name="phone" value="<%= contact.phone %>">
                  </div>
                  <div class="form-group col-md-6">
                    <label>email</label>
                    <input type="email" class="form-control" name="email" value="<%= contact.email %>">
                  </div>
                </div>
                <br>
                <button type="submit" class="btn btn-warning">Editar</button>
                <button type="submit" class="btn btn-secondary" href="/">Cancelar</button>
            </form>
    </body>
</html>